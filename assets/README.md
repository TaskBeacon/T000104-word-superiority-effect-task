# Assets

This task uses PsychoPy drawing primitives only. Four-slot letter arrays, the postdisplay patterned mask, and the forced-choice probe are generated at runtime; no external media files are required.
